(function(){
'use strict';
const Store=window.MyBodyStore;if(!Store)return;
const $=(s,r=document)=>r.querySelector(s);
const n=(v,f=0)=>{const x=Number(v);return Number.isFinite(x)?x:f};
let state=Store.read(),wired=false,queued=false;
function photos(){try{const key=`mybody.progressPhotos.v1.${localStorage.getItem(Store.SESSION_KEY)||'guest'}`;const p=JSON.parse(localStorage.getItem(key)||'[]');return Array.isArray(p)?p:[]}catch{return[]}}
function photoSrc(p){return p?.dataUrl||p?.data||p?.src||p?.url||''}
function photoDate(p){return p?.date||p?.createdAt?.slice?.(0,10)||''}
function weightRows(){return (state.weights||[]).filter(x=>x.date&&n(x.weight)>0).slice().sort((a,b)=>String(a.date).localeCompare(String(b.date)))}
function waistRows(){return (state.abdomen||[]).filter(x=>x.date&&(n(x.value)>0||n(x.inches)>0)).slice().sort((a,b)=>String(a.date).localeCompare(String(b.date))).map(x=>({date:x.date,value:n(x.value)||n(x.inches)*2.54}))}
function strengthRows(){const out=[];Object.keys(state.workoutLog||{}).sort().forEach(date=>{let best=0;Object.values(state.workoutLog?.[date]||{}).forEach(log=>(log?.exercises||[]).forEach(ex=>(ex?.setsDetail||[]).forEach(s=>{const w=n(s.actualWeight),r=n(s.actualReps);if(w>0&&r>0)best=Math.max(best,r<=15?w*(1+r/30):w)})));if(best)out.push({date,value:best})});return out}
function recentMilestones(){const w=weightRows(),wa=waistRows(),s=strengthRows(),items=[];if(w.length>1){const first=w[0],last=w[w.length-1],d=last.weight-first.weight;if(Math.abs(d)>=.2)items.push({date:last.date,title:`Weight ${d<0?'down':'up'} ${Math.abs(d).toFixed(1)} kg`,type:'weight'})}if(wa.length>1){const d=wa.at(-1).value-wa[0].value;if(Math.abs(d)>=.5)items.push({date:wa.at(-1).date,title:`Waist ${d<0?'down':'up'} ${Math.abs(d).toFixed(1)} cm`,type:'waist'})}if(s.length>1){const first=s[0].value,last=Math.max(...s.map(x=>x.value)),pct=first?((last-first)/first*100):0;if(pct>=2)items.push({date:s.at(-1).date,title:`Strength best +${Math.round(pct)}%`,type:'strength'})}return items.slice(0,4)}
function ensureCard(id,cls,anchor){let card=$('#'+id);if(!card){card=document.createElement('section');card.id=id;card.className='card '+cls;anchor?.insertAdjacentElement('afterend',card)}return card}
function renderTimeline(){const host=$('#progress');if(!host)return;const anchor=$('#experienceJourney')||$('#p4Projection')||host.querySelector('.stats-grid'),card=ensureCard('experienceTimeline','experience-timeline',anchor);if(!card)return;const w=weightRows(),wa=waistRows(),m=recentMilestones(),latestW=w.at(-1)?.weight,latestWa=wa.at(-1)?.value;const html=`<div class="experience-section-head"><div><span>Transformation timeline</span><h3>Your change, in context</h3></div></div><div class="timeline-summary"><div><small>Weight</small><strong>${latestW?latestW.toFixed(1)+' kg':'—'}</strong><span>${w.length>1?(latestW-w[0].weight>0?'+':'')+(latestW-w[0].weight).toFixed(1)+' kg total':'Add more entries'}</span></div><div><small>Waist</small><strong>${latestWa?latestWa.toFixed(1)+' cm':'—'}</strong><span>${wa.length>1?(latestWa-wa[0].value>0?'+':'')+(latestWa-wa[0].value).toFixed(1)+' cm total':'Add more entries'}</span></div></div><div class="timeline-milestones">${m.length?m.map(x=>`<article><i class="${x.type}"></i><div><strong>${x.title}</strong><span>${x.date}</span></div></article>`).join(''):'<p class="empty">Log weight, waist and workouts to build your transformation timeline.</p>'}</div>`;if(card.dataset.renderKey!==html){card.dataset.renderKey=html;card.innerHTML=html}}
function renderCompare(){const host=$('#progress');if(!host)return;const anchor=$('#phase2Photos')||$('.p2-photos',host)||$('#experienceTimeline'),card=ensureCard('experienceCompare','experience-compare',anchor);if(!card)return;const p=photos().filter(x=>photoSrc(x));let html;if(p.length<2)html='<div class="experience-section-head"><div><span>Before & after</span><h3>Visual comparison</h3></div></div><p class="empty">Add at least two progress photos to unlock side-by-side comparison.</p>';else{const first=p[0],last=p[p.length-1],a=photoSrc(first),b=photoSrc(last);html=`<div class="experience-section-head"><div><span>Before & after</span><h3>Visual comparison</h3></div><small>${photoDate(first)} → ${photoDate(last)}</small></div><div class="compare-stage"><img src="${a}" alt="Earlier progress photo"><div class="compare-after" style="width:50%"><img src="${b}" alt="Latest progress photo"></div><span class="compare-label before">Before</span><span class="compare-label after">Latest</span></div><input class="compare-range" type="range" min="0" max="100" value="50" aria-label="Compare before and after photos">`}if(card.dataset.renderKey!==html){card.dataset.renderKey=html;card.innerHTML=html;const range=$('.compare-range',card);if(range)range.oninput=e=>{$('.compare-after',card).style.width=`${e.target.value}%`}}}
function approxBurn(){[['#dashBurn',true],['#baseBurn',false],['#stepBurn',false],['#exerciseBurn',false]].forEach(([sel,bare])=>{const el=$(sel);if(!el)return;const raw=parseFloat(String(el.textContent).replace(/[^0-9.-]/g,''));if(!Number.isFinite(raw))return;const rounded=Math.round(raw/50)*50,next=bare?`~${rounded}`:`~${rounded} kcal`;if(el.textContent!==next)el.textContent=next});const bal=$('#dashBalance');if(bal){const raw=parseFloat(String(bal.textContent).replace(/[^0-9.-]/g,''));if(Number.isFinite(raw)){const next=`~${Math.round(raw/50)*50}`;if(bal.textContent!==next)bal.textContent=next}}}
function render(){queued=false;state=Store.read();renderTimeline();renderCompare();setTimeout(approxBurn,40)}
function schedule(ms=0){if(ms){setTimeout(()=>schedule(),ms);return}if(queued)return;queued=true;requestAnimationFrame(render)}
function wire(){if(wired)return;wired=true;window.addEventListener('mybody:state',()=>schedule(100));document.addEventListener('click',e=>{if(e.target.closest('#progress-tab,[data-tab="progress"],[data-nav="progress"],#saveDaily,#saveWorkout,.p2-photo-add,.p2-photo-delete'))schedule(140)},true)}
function init(){render();wire();setTimeout(()=>schedule(),350)}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();
})();